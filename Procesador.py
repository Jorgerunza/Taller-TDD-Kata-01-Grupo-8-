
class Procesador:
        def contador (self,lista):
            if (lista == ''):
                return [0,0]
            else:
                numeros = str.split( lista, ',')
                respuesta ={len(numeros)}
                return respuesta
